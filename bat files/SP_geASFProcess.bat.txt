@echo off
REM Navigate to the desired directory
cd /d "C:\Users\2097893\OneDrive - Giant Eagle, Inc\Team Documents\Shreya KT\GE Modules" || (
    echo Failed to navigate to the directory.
    pause
    exit /b
)

REM Activate the virtual environment
call .\.venv\Scripts\activate.bat || (
    echo Failed to activate the virtual environment.
    pause
    exit /b
)

REM Navigate to the project directory
cd geASFProcess || (
    echo Failed to navigate to the project directory.
    pause
    exit /b
)

REM Run the Django server
py manage.py runserver || (
    echo Failed to run the server.
    pause
    exit /b
)

REM Keep the window open after execution
pause
